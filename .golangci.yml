linters:
  enable:
    - errcheck      # no swallowed errors
    - errorlint     # errors compared with == instead of errors.Is/As
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused

issues:
  exclude-rules:
    - path: _test\.go
      linters: [errcheck]
