FROM golang:1.24-alpine AS builder
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o /dev .

FROM alpine:3.21
RUN apk add --no-cache docker-cli docker-compose
COPY --from=builder /dev /usr/local/bin/dev
WORKDIR /workspace
ENTRYPOINT ["dev"]
